# Cloud Infrastructure Automation — AWS + Terraform

Automated cloud provisioning using Terraform, wrapped in a Python orchestrator to manage state and configurations.

## Features
- Python orchestration script (`provision.py`)
- Standardized Terraform modules
- Automatic state management

## Usage
```bash
python provision.py --env prod --action apply
```