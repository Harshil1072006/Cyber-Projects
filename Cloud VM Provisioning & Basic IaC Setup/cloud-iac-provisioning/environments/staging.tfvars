environment      = "staging"
project_name     = "sre-iac-demo"
aws_region       = "us-east-1"
instance_type    = "t3.micro"  

# SECURITY WARNING: Must be set to a secure CIDR range.
allowed_ssh_cidr = "" # Intentionally left blank to force user to define it for staging.
